name: "CI: Visualizer Image Cleanup"

"on":
  # Weekly on Thursday at 03:41 UTC
  schedule:
    - cron: "41 3 * * 4"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ghcr-cleanup:
    name: GHCR cleanup
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Cleanup old images
        uses: ghcr-manager/ghcr-cleanup-manager@5d513e90945c4cb8c8094911c7ee18f1a37db29c # v1.1.11
        with:
          command: cleanup
          token: ${{ github.token }}
          owner: ${{ github.repository_owner }}
          package: ${{ github.event.repository.name }}-visualizer
          upload-artifacts: true
          use-regex: true
          # delete-tags: '^(?!(latest|v[0-9]+|v[0-9]+\.[0-9]+\.[0-9]+)$).+'
          exclude-tags: |
            ^latest$
            ^v[0-9]+$
            ^v[0-9]+\.[0-9]+\.[0-9]+$
          keep-n-tagged: 1
          delete-untagged: true
          scan-after-cleanup: "true"
