{
  "name": "ghcr-cleanup-manager",
  "description": "GHCR Cleanup Manager: GitHub Action and CLI for safe GitHub Container Registry cleanup and inspection.",
  "homepage": "https://github.com/ghcr-manager/ghcr-cleanup-manager#readme",
  "version": "v1.1.0",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ghcr",
    "github-container-registry",
    "github-packages",
    "container-registry",
    "docker",
    "oci",
    "cleanup",
    "github-actions",
    "sqlite"
  ],
  "bugs": {
    "url": "https://github.com/ghcr-manager/ghcr-cleanup-manager/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ghcr-manager/ghcr-cleanup-manager"
  },
  "workspaces": [
    "visualizer"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "files": [
    "dist",
    "resources/sql",
    "LICENSE",
    "README.md",
    "CHANGELOG.md"
  ],
  "bin": {
    "ghcr-cleanup-manager": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json && npm run build:visualizer",
    "build:visualizer": "npm run build --workspace visualizer",
    "ghcr-cleanup-manager": "tsx src/cli/index.ts",
    "ghcr-cleanup-manager:dist": "node dist/cli/index.js",
    "visualize": "npm run start --workspace visualizer --",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm run check:file-names && npm run check:test-mapping && npm run typecheck && npm run typecheck --workspace visualizer && npm run lint:ts && npm run lint:yaml && npm run lint:markdown && npm run lint --workspace visualizer && npm run format:check",
    "check:file-names": "node tools/tests/check-src-file-names.mjs",
    "check:test-mapping": "node tools/tests/check-test-mapping.mjs",
    "lint:ts": "eslint \"src/**/*.ts\" \"tests/**/*.ts\"",
    "lint:yaml": "eslint \".github/**/*.yml\" \"*.yml\"",
    "lint:markdown": "markdownlint-cli2",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "test": "bash -lc 'shopt -s globstar nullglob; node --import tsx --test tests/**/*.test.ts' && npm run test --workspace visualizer",
    "coverage": "bash -lc 'shopt -s globstar nullglob; c8 --src src --reporter=text --reporter=text-summary node --import tsx --test tests/**/*.test.ts'"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.12.4",
    "c8": "^11.0.0",
    "eslint": "^10.2.1",
    "eslint-plugin-yml": "^3.4.0",
    "globals": "^17.5.0",
    "markdownlint-cli2": "^0.22.1",
    "prettier": "^3.8.3",
    "tsx": "^4.20.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.46.2",
    "yaml": "^2.9.0"
  }
}
